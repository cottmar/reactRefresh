// import React from 'react';

// HIGHER ORDER COMPONENT, WRAPS ANOTHER COMPONENT

// DOESN'T CONTAIN ITS OWN LOGIC OR STYLING, JUST A TECHNICAL WRAPPER

// the only thing we return are props.children
const aux = props => props.children

export default aux;